{
  "name": "contact-manager",
  "private": true,
  "version": "0.0.1",
  "description": "Contact Manager",
  "license": "MIT",
  "devDependencies": {    
    "bower": "^1.3.1"  
  },
  "scripts": {
    "postinstall": "bower update",
    "prestart": "npm update"
  }
}
